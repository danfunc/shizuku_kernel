if(SHIZUKU_CPU_ARCH STREQUAL "rp2040")
  set(SHIZUKU_CPU_ARCH_VALID
      YES
      CACHE BOOL "SHIZUKU_CPU_VALID RESULT" FORCE)
endif()
set(SHIZUKU_MEMORY_MANAGER
    "pico_sdk_memory_manager"
    CACHE STRING "" FORCE)
